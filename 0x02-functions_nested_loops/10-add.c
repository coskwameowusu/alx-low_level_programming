#include "main.h"
/**
 * add -  adds two integers and returns the result.
 * @b: int to check
 * @a: int to check
 * Return: every minute of the day
 */

int add(int a, int b)
{
int c;
c = a + b;
return (c);
}
